package main

import "fmt"

func main() {

	// var n int
	// fmt.Println("Enter the value: ")
	// fmt.Scan(&n)

	// nInFloat := float64(n)

	// fmt.Printf("Integer: %d and FLoat: %.2f\n", n, nInFloat)

	// fmt.Println("Enter the float value: ")
	// var floatValue float64
	// fmt.Scan(&floatValue)

	// floatInInt := int(floatValue)

	// fmt.Printf("Float: %f and Integer: %d\n", floatValue, floatInInt)

	// fmt.Println("Enter the string: ")
	// var s string
	// fmt.Scan(&s)
	// fmt.Println(reflect.TypeOf(s))

	// stringInInt, _ := strconv.Atoi(s)

	// fmt.Printf("String: %s and Integer: %v", s, stringInInt)

	//

	// fmt.Println(strconv.ParseBool("true"))

	fmt.Println("Enter the three values")
	var a, b, c int
	fmt.Scan(&a)
	fmt.Scan(&b)
	fmt.Scan(&c)

	fmt.Println(a, b, c)

}
